cmake_minimum_required(VERSION 3.6)
project(WaifuBot)
if(DEFINED ENV{BOOST_INCL} AND DEFINED ENV{BOOST_STAGE})
	subdirs(source)
else()
	message(SEND_ERROR "ERROR: Boost include and staging directories are undefined")
	message(FATAL_ERROR "Please set the following environment variables: BOOST_INCL and BOOST_STAGE")
	return()
endif()
